# Created: 2026-03-24 21:58:34
# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This project is in its initial setup phase. No source files, build configuration, or dependencies have been added yet.

## Environment Variables

API keys and secrets are stored in `.env` files (currently `.env.rtf` contains `OPENROUTER_API_KEY`). Load environment variables from `.env` before running any code that calls external APIs.
